## Wnd:

a) very reliable: app, cls
b) alright: title

Maintain at least part of `title` in <wnd> selectors.
Before wildcarding window attributes, scan Window Level Context for other elements that would also match — if wildcarding collides with other open windows, use more specific attribute or tighter wildcard.