# UiPath Studio local/build artifacts
.local/
.settings/
.storage/
.tmh/
project.local.json

# Not project content: a full nested skills-repo clone that broke the
# Workflow Analyzer during Publish (deep paths, huge file count)
.uipath/.skills/

# Compiled coded-workflow output (regenerated by Studio on build)
*.pdb
Application*.Core.xml

# Build/test output
bin/
obj/
TestResults/
*.trx

# OS/editor cruft
Thumbs.db
.DS_Store
*.swp
